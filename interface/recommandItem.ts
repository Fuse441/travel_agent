/* eslint-disable prettier/prettier */
export interface RecommandItem {
    name: string;
    rating: number;
    review_count: number;
    photos: string[];
    recommended_age_group: string;
  address: string;
}